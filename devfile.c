// this the another file but which is in the other branch that is devbranch 
// this the same repo but with different branch
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("enter the numbers");
    scanf("%d %d",&a &b);
    sum=a+b;
    printf("sum = %d",sum);
} 